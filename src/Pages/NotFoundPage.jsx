import Hero from "../components/Hero";
import NotFound from "../components/NotFound";

const NotFoundPage = () => {
  return (
    <div>
      <Hero />
      <NotFound />
    </div>
  );
};

export default NotFoundPage;
